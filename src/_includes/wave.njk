{# Waterline divider — set `waveClass` before including (wave--foam | wave--deep).
   fill=currentColor so the color always resolves from the token contract. #}
<div class="wave {{ waveClass }}" aria-hidden="true">
  <svg viewBox="0 0 1440 64" preserveAspectRatio="none" focusable="false" role="presentation">
    <path fill="currentColor" d="M0,32 C240,64 480,0 720,32 C960,64 1200,0 1440,32 L1440,64 L0,64 Z"></path>
  </svg>
</div>
